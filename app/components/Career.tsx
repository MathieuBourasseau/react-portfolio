import React from 'react'
import { Background } from "../types/background"

type CareerProps = {
  activeBackground: Background
}

export default function Career({ activeBackground}) {
  return (
    <div>
      <h1>Mon parcours</h1>
    </div>
  )
}
